<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Car-Track" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%# Slot opcional p/ páginas injetarem conteúdo no <head> %>
    <%= yield :head %>

    <%# PWA (opcional) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/car_icon.png" type="image/png">
    <link rel="icon" href="/car_icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/car_icon.png">

    <!-- Bootstrap CSS (CDN) -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

    <!-- Font Awesome (CDN) -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" rel="stylesheet">

    <!-- Chart.js (CDN) -->
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

    <!-- CSS da aplicação via Asset Pipeline -->
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>

    <!-- JS via Importmap (carrega app/javascript/application.js que importa Turbo + controllers) -->
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <div class="container-fluid">
      <div class="row">
        <!-- Sidebar fixo -->
        <nav class="col-md-3 col-lg-2 sidebar d-md-flex flex-column p-3 border-end" style="min-height:100vh;">
          <div>
            <div class="sidebar-header mb-4">
              <h6 class="ms-1 text-uppercase text-muted">CAR PRICE TRACKER</h6>
              <span class="ms-1 d-block text-secondary small">Menu</span>
            </div>

            <!-- Área do usuário (Devise) -->
            <div class="card shadow-sm border-0 mb-3">
              <div class="card-body d-flex align-items-center gap-3">
                <div class="rounded-circle bg-secondary text-white d-flex align-items-center justify-content-center"
                    style="width:44px;height:44px;">
                  <% if user_signed_in? %>
                    <% iniciais = (current_user.try(:nome) || current_user.email).to_s.split.map{|p| p[0]}.join.upcase.first(2) %>
                    <span class="fw-semibold"><%= iniciais %></span>
                  <% else %>
                    <i class="fas fa-user"></i>
                  <% end %>
                </div>

                <div class="flex-grow-1">
                  <% if user_signed_in? %>
                    <div class="fw-semibold">
                      <%= current_user.try(:nome).presence || current_user.email %>
                    </div>
                    <div class="small text-muted">Conectado</div>
                  <% else %>
                    <div class="fw-semibold">Visitante</div>
                    <div class="small text-muted">Não autenticado</div>
                  <% end %>
                </div>
              </div>

              <div class="list-group list-group-flush">
                <% if user_signed_in? %>
                  <%= link_to edit_user_registration_path, class: "list-group-item list-group-item-action d-flex align-items-center" do %>
                    <i class="fas fa-user-cog me-2"></i> Editar perfil
                  <% end %>

                  <%= button_to destroy_user_session_path,
                            method: :delete,
                            class: "list-group-item list-group-item-action d-flex align-items-center text-danger",
                            form: { data: { turbo_confirm: "Deseja sair?" }, class: "p-0 m-0 border-0 bg-transparent" } do %>
                   <i class="fas fa-right-from-bracket me-2"></i> Sair
                  <% end %>

                <% else %>
                  <%= link_to new_user_session_path, class: "list-group-item list-group-item-action d-flex align-items-center" do %>
                    <i class="fas fa-right-to-bracket me-2"></i> Entrar
                  <% end %>

                  <%= link_to new_user_registration_path, class: "list-group-item list-group-item-action d-flex align-items-center" do %>
                    <i class="fas fa-user-plus me-2"></i> Criar conta
                  <% end %>
                <% end %>
              </div>
            </div>


            <% if notice.present? %>
              <div class="alert alert-success py-2"><%= notice %></div>
            <% end %>
            <% if alert.present? %>
              <div class="alert alert-danger py-2"><%= alert %></div>
            <% end %>

            <% if user_signed_in? %>
              <div class="nav flex-column nav-links gap-2 mt-3">
                <%= link_to root_path, class: "nav-link d-flex align-items-center #{current_page?(root_path) ? 'active fw-semibold' : ''}" do %>
                  <i class="fas fa-home me-2"></i> Início
                <% end %>

                <%= link_to marcas_path, class: "nav-link d-flex align-items-center #{controller_name == 'marcas' ? 'active fw-semibold' : ''}" do %>
                  <i class="fas fa-car me-2"></i> Marcas
                <% end %>

                <%# Ajuste o path do comparador quando existir %>
                <%= link_to comparador_path, class: "nav-link d-flex align-items-center" do %>
                  <i class="fas fa-cogs me-2"></i> Comparador
                <% end %>
              </div>
            <% end %>
          </div>

          <div class="sidebar-footer mt-auto text-muted small">
            &copy; <%= Time.current.year %> Car Price Tracker
          </div>
        </nav>

        <!-- Conteúdo -->
        <main class="col-md-9 ms-sm-auto col-lg-10 main-content p-4">
          <%= yield %>
        </main>
      </div>
    </div>

    <!-- Bootstrap JS (opcional: para componentes como dropdown/modal/toast) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

    <%# Slot para scripts específicos da página (gráficos etc.) %>
    <%= yield :scripts %>
  </body>
</html>
